1.hadoop

- hdfs:
	- fsimage editlog的更新逻辑
	- 数据块存放逻辑,写入机制重要
	- hdfs上的基本命令
	- 节点通信机制不重要，通信端口配置重要
- yarn
	- 和MapReduce1.0的对比
	- 每个应用的管理放到worker node而不是master node
	- yarn命令行
- MapReduce
	- 计算向数据靠拢
	- 任务分割
	- shuffle的目的、触发条件和触发时机重要，具体缓冲区大小等等细节不重要
	- 基于MapReduce的数据库操作，自然连接
	- combiner函数


2.spark

- DAG与血缘
- RDD运算，宽依赖、窄依赖与stage,transformation与action,
- 各组件的功能
- spark的运行模式：本地模式、stand alone(使用spark的资源管理器）、cluster
- spark shell


3.scala2

- 读懂基本语法:流程控制、卫语句、推导式
- 基本数据结构：容器类
- 面向对象：
	- 构造函数（去年考了）、构造器调用顺序,
	- object(程序入口、伴生对象)
	- trait的使用
	- 抽象类、抽象对象、抽象方法
	- 函数的定义和使用、高阶函数重要
- 能写breeze
- spark的常用api,RDD操作的api很重要


4.随机模拟：读懂代码并且能算出运行结果

- EM
- MCMC
- 线性模型
- slice

选择题：组件功能，20～30分
解答题：10~20分
程序题：40分以上