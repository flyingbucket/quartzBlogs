朴素贝叶斯分类器的核心假设是**特征条件独立性假设**，即假设给定类别后，特征之间是独立的。对于 $n$ 个特征 $x_1, x_2, ..., x_n$，在类别 $C$ 的条件下，条件概率 $P(X|C)=P(x_1, x_2, ..., x_n | C)$ 可以分解为：

$$
P(X|C)=P(x_1, x_2, ..., x_n | C) = \prod_{i=1}^{n} P(x_i | C)
$$

对于各维度离散取值，则用已知数据的频率来计算$P(x_i|C)$,对于连续的情况，可以常见的建模方法有高斯假设和分箱离散化。